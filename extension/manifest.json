{
  "manifest_version": 3,
  "name": "creel bridge",
  "version": "0.2.0",
  "description": "Lets creel agents drive cross-origin web pages the static creel page cannot reach on its own.",
  "permissions": ["tabs", "scripting", "activeTab"],
  "host_permissions": ["<all_urls>"],
  "background": { "service_worker": "background.js" },
  "content_scripts": [
    {
      "//": "The connector is injected ONLY on creel origins — this is the sole surface that can command the bridge. host_permissions stays broad so the bridge can ACT on any site; the two are independent. Remove the localhost entries for a production-only build.",
      "matches": [
        "https://scbrown.github.io/creel/*",
        "http://localhost/*",
        "http://127.0.0.1/*"
      ],
      "js": ["creel-connector.js"],
      "run_at": "document_start"
    }
  ],
  "icons": { "128": "icons/icon-128.png" }
}
